{
  "name": "knowledge-graph",
  "scripts": {
    "build": "tsc ui.ts --target ES2020 --lib ES2020,DOM --moduleResolution node --outDir . --strict",
    "dev": "npx serve . -p 3000"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
